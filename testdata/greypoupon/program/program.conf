# Note that broadcastinator loads each program in the order described below
programs = [
    # The full title of the show, followed by the path to that program's config file
    { title="Big Buck Bunny", conf="bbb/big-buck-bunny.conf", runtime="10m34.6s" },
    { title="Steamboat Willie", conf="willy/steamboat-willie.conf", runtime="07m46.66s" },
]

# Number of programs that play before a routine programming schedule is played
show-schedule-after = 2

# 